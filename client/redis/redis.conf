# Minimal Redis config for GoGaze (mounted by docker-compose.prod.yml).
# On an internal Docker network only; set `requirepass` for untrusted networks.
bind 0.0.0.0
protected-mode no
maxmemory 100mb
maxmemory-policy allkeys-lru
appendonly yes
